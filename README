Snap blogs site - based on Jekyll

Assuming you have Ruby and Jekyll installed, you can serve the site locally by
running:

    jekyll serve --config _config_development.yml --drafts --watch

Then navigating to:

    http://localhost:4000
